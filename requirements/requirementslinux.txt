# ── Mycroft Phoenix - Requirements Linux ──
# Installer les packages systeme AVANT :
#   Debian/Ubuntu: sudo apt install portaudio19-dev mpg123 flac libfann-dev
#   Fedora: sudo dnf install portaudio-devel mpg123 flac fann-devel
#   Arch: sudo pacman -S portaudio mpg123 flac fann

# ── Core ──
requests~=2.26.0
gTTS>=2.2.2,<2.3.0
PyAudio==0.2.11
pyee==8.1.0
SpeechRecognition==3.8.1
tornado~=6.1
websocket-client~=1.2.1
requests-futures==0.9.5
pyserial==3.0
psutil==5.6.6
pillow==8.3.2
python-dateutil==2.6.0
fasteners==0.14.1
PyYAML~=5.4

# ── Mycroft ──
lingua-franca==0.4.2
msm==0.8.9
msk==0.3.16
mycroft-messagebus-client==0.9.4
adapt-parser==0.5.1
padatious-phoenix
padaos==0.1.9
petact==0.1.2

# ── Linux-specific ──
pyxdg==0.27

# ── Voice (remplace precise-runner) ──
vosk
piper-tts

# ── Optionnel: audio ──
python-vlc==1.1.2
pyalsaaudio==0.8.2
